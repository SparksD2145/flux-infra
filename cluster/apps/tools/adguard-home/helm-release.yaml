---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: portainer
  namespace: tools
spec:
  interval: 10m
  chart:
    spec:
      chart: portainer
      version: 1.0.18
      sourceRef:
        kind: HelmRepository
        name: portainer-charts
        namespace: flux-system
      interval: 10m
  values:
    service:
      type: ClusterIP
    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: nginx-internal
      hosts:
        - host: portainer.sparks.codes
          paths:
            - path: /
              pathType: Prefix
      tls:
        - secretName: acme-crt-secret-sparks-codes
          hosts:
            - portainer.sparks.codes
    persistence:
      storageClass: ceph-block
