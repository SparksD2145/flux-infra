---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-k6-influxdb-pvc
  namespace: home
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: ceph-block
