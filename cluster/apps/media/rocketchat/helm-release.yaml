---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: rocketchat
  namespace: media
spec:
  interval: 15m
  chart:
    spec:
      chart: rocketchat
      version: 5.3.2
      sourceRef:
        kind: HelmRepository
        name: rocketchat-charts
        namespace: flux-system
      interval: 15m
  values:
    host: chat.sparks.codes
    mongodb:
      auth:
        passwords:
          - "${MEDIA_ROCKETCHAT_PASSWORD}"
        rootPassword: "${MEDIA_ROCKETCHAT_PASSWORD}"
    persistence:
      enabled: true
      size: 24G
    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: nginx-external
        hajimari.io/enable: "false"
      tls:
        - hosts:
            - "chat.sparks.codes"
          secretName: acme-crt-secret-sparks-codes
