---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: local-tubesync-config
spec:
  claimRef:
    name: local-tubesync-config
    namespace: media
  accessModes:
    - ReadWriteMany
  capacity:
    storage: 2Gi
  local:
    path: /mnt/media-vault/media.sparks.codes/media/Youtube/config
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - tango

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-tubesync-config
  namespace: media
spec:
  volumeName: local-tubesync-config
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 2Gi
